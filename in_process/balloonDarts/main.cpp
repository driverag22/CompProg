#include "iostream"
#include <algorithm>
#include <climits>
#include <cstdio>
#include <bits/stdc++.h>
#include "vector"
using namespace std;

int main() {
  int n, i, j;
  cin >> n;
  int points[n][2];
  for (i = 0; i < n; i++) {
    cin >> points[i][0] >> points[i][1];
  }
  for (i = 0; i < 10000; i++) {
    
  }
  return 0;
}
