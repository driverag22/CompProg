#include "iostream"
#include <algorithm>
#include <climits>
#include <cstdio>
#include <bits/stdc++.h>
#include "vector"
using namespace std;

void oneRun(){
    int H, V, i, x, y, j;
    string word;
    cin >> H >> V;
    char board[H][V];
    memset(board, 0, H * V * sizeof(board[0][0]));
}
int main() {
    int cases; cin >> cases;
    while(cases-- > 0) oneRun(); 
    return 0;
}
